-- MySQL Script generated by MySQL Workbench
-- Tue Feb 17 16:53:45 2026
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

-- -----------------------------------------------------
-- Schema mydb
-- -----------------------------------------------------
-- -----------------------------------------------------
-- Schema contralog
-- -----------------------------------------------------

-- -----------------------------------------------------
-- Schema contralog
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `contralog` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci ;
USE `contralog` ;

-- -----------------------------------------------------
-- Table `contralog`.`cargos`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `contralog`.`cargos` (
  `id_cargo` INT NOT NULL AUTO_INCREMENT,
  `codigo_cargo` VARCHAR(15) NOT NULL,
  `nombre_cargo` VARCHAR(50) NOT NULL,
  `descripcion` VARCHAR(255) NULL DEFAULT NULL,
  PRIMARY KEY (`id_cargo`),
  UNIQUE INDEX `nombre_cargo` (`nombre_cargo` ASC) VISIBLE,
  UNIQUE INDEX `ix_cargos_codigo_cargo` (`codigo_cargo` ASC) VISIBLE,
  INDEX `ix_cargos_id_cargo` (`id_cargo` ASC) VISIBLE)
ENGINE = InnoDB
AUTO_INCREMENT = 3
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `contralog`.`usuarios`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `contralog`.`usuarios` (
  `id_usuario` INT NOT NULL AUTO_INCREMENT,
  `codigo_usuario` VARCHAR(25) NOT NULL,
  `nombre` VARCHAR(100) NOT NULL,
  `email` VARCHAR(150) NOT NULL,
  `password` VARCHAR(255) NOT NULL,
  `rol` ENUM('admin', 'aprobador', 'funcionario') NOT NULL,
  `estado` ENUM('activo', 'inactivo') NULL DEFAULT NULL,
  `id_cargo` INT NULL DEFAULT NULL,
  `ultimo_ingreso` TIMESTAMP NULL DEFAULT NULL,
  `created_at` TIMESTAMP NULL DEFAULT now(),
  PRIMARY KEY (`id_usuario`),
  UNIQUE INDEX `ix_usuarios_codigo_usuario` (`codigo_usuario` ASC) VISIBLE,
  UNIQUE INDEX `ix_usuarios_email` (`email` ASC) VISIBLE,
  INDEX `id_cargo` (`id_cargo` ASC) VISIBLE,
  INDEX `ix_usuarios_id_usuario` (`id_usuario` ASC) VISIBLE,
  CONSTRAINT `usuarios_ibfk_1`
    FOREIGN KEY (`id_cargo`)
    REFERENCES `contralog`.`cargos` (`id_cargo`)
    ON DELETE SET NULL)
ENGINE = InnoDB
AUTO_INCREMENT = 2
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `contralog`.`auditoria_cambios`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `contralog`.`auditoria_cambios` (
  `id_log` INT NOT NULL AUTO_INCREMENT,
  `id_usuario` INT NULL DEFAULT NULL,
  `accion` VARCHAR(20) NOT NULL,
  `tabla_afectada` VARCHAR(50) NOT NULL,
  `id_registro_afectado` INT NULL DEFAULT NULL,
  `detalle_cambio` JSON NULL DEFAULT NULL,
  `fecha_cambio` TIMESTAMP NULL DEFAULT now(),
  PRIMARY KEY (`id_log`),
  INDEX `id_usuario` (`id_usuario` ASC) VISIBLE,
  INDEX `ix_auditoria_cambios_id_log` (`id_log` ASC) VISIBLE,
  CONSTRAINT `auditoria_cambios_ibfk_1`
    FOREIGN KEY (`id_usuario`)
    REFERENCES `contralog`.`usuarios` (`id_usuario`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `contralog`.`clientes`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `contralog`.`clientes` (
  `id_cliente` INT NOT NULL AUTO_INCREMENT,
  `codigo_cliente` VARCHAR(20) NOT NULL,
  `tipo_persona` ENUM('natural', 'juridica') NOT NULL,
  `tipo_identificacion` ENUM('NIT', 'CC', 'CE', 'RUT') NOT NULL,
  `identificacion_fiscal` VARCHAR(20) NOT NULL,
  `nombre_razon_social` VARCHAR(150) NOT NULL,
  `email_contacto` VARCHAR(150) NULL DEFAULT NULL,
  `telefono` VARCHAR(25) NULL DEFAULT NULL,
  `direccion` VARCHAR(255) NULL DEFAULT NULL,
  `estado` ENUM('activo', 'inactivo') NULL DEFAULT NULL,
  `created_at` TIMESTAMP NULL DEFAULT now(),
  PRIMARY KEY (`id_cliente`),
  UNIQUE INDEX `ix_clientes_identificacion_fiscal` (`identificacion_fiscal` ASC) VISIBLE,
  UNIQUE INDEX `ix_clientes_codigo_cliente` (`codigo_cliente` ASC) VISIBLE,
  INDEX `ix_clientes_id_cliente` (`id_cliente` ASC) VISIBLE,
  INDEX `ix_clientes_nombre_razon_social` (`nombre_razon_social` ASC) VISIBLE)
ENGINE = InnoDB
AUTO_INCREMENT = 2
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `contralog`.`contratos`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `contralog`.`contratos` (
  `id_contrato` INT NOT NULL AUTO_INCREMENT,
  `codigo_unico` VARCHAR(25) NOT NULL,
  `id_cliente` INT NOT NULL,
  `creado_por` INT NOT NULL,
  `tipo_contrato` ENUM('natural', 'juridica') NOT NULL,
  `fecha_inicio` DATE NULL DEFAULT NULL,
  `fecha_final` DATE NULL DEFAULT NULL,
  `estado` ENUM('vencido', 'activo', 'por_vencer', 'cancelado', 'pendiente') NULL DEFAULT NULL,
  `nda_firmado` TINYINT(1) NULL DEFAULT NULL,
  `s3_key_nda` VARCHAR(255) NULL DEFAULT NULL,
  PRIMARY KEY (`id_contrato`),
  UNIQUE INDEX `ix_contratos_codigo_unico` (`codigo_unico` ASC) VISIBLE,
  INDEX `id_cliente` (`id_cliente` ASC) VISIBLE,
  INDEX `creado_por` (`creado_por` ASC) VISIBLE,
  INDEX `ix_contratos_id_contrato` (`id_contrato` ASC) VISIBLE,
  CONSTRAINT `contratos_ibfk_1`
    FOREIGN KEY (`id_cliente`)
    REFERENCES `contralog`.`clientes` (`id_cliente`),
  CONSTRAINT `contratos_ibfk_2`
    FOREIGN KEY (`creado_por`)
    REFERENCES `contralog`.`usuarios` (`id_usuario`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `contralog`.`tipo_documentos`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `contralog`.`tipo_documentos` (
  `id_tipo` INT NOT NULL AUTO_INCREMENT,
  `nombre` VARCHAR(100) NOT NULL,
  PRIMARY KEY (`id_tipo`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `contralog`.`contrato_documentos`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `contralog`.`contrato_documentos` (
  `id_documento` INT NOT NULL AUTO_INCREMENT,
  `codigo_seguimiento` VARCHAR(25) NOT NULL,
  `id_contrato` INT NOT NULL,
  `id_tipo_doc` INT NOT NULL,
  `s3_key` VARCHAR(255) NOT NULL,
  `fecha_subida` TIMESTAMP NULL DEFAULT now(),
  PRIMARY KEY (`id_documento`),
  UNIQUE INDEX `codigo_seguimiento` (`codigo_seguimiento` ASC) VISIBLE,
  INDEX `id_contrato` (`id_contrato` ASC) VISIBLE,
  INDEX `id_tipo_doc` (`id_tipo_doc` ASC) VISIBLE,
  CONSTRAINT `contrato_documentos_ibfk_1`
    FOREIGN KEY (`id_contrato`)
    REFERENCES `contralog`.`contratos` (`id_contrato`)
    ON DELETE CASCADE,
  CONSTRAINT `contrato_documentos_ibfk_2`
    FOREIGN KEY (`id_tipo_doc`)
    REFERENCES `contralog`.`tipo_documentos` (`id_tipo`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `contralog`.`proyectos`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `contralog`.`proyectos` (
  `id_proyecto` INT NOT NULL AUTO_INCREMENT,
  `id_cliente` INT NOT NULL,
  `id_contrato` INT NULL DEFAULT NULL,
  `nombre_proyecto` VARCHAR(150) NOT NULL,
  `etapa` ENUM('prospeccion', 'contacto', 'propuesta', 'negociacion', 'ejecucion', 'finalizado') NULL DEFAULT NULL,
  `horas_estimadas` FLOAT NULL DEFAULT NULL,
  `presupuesto_inicial` DECIMAL(15,2) NULL DEFAULT NULL,
  `fecha_inicio` DATE NULL DEFAULT NULL,
  PRIMARY KEY (`id_proyecto`),
  INDEX `id_cliente` (`id_cliente` ASC) VISIBLE,
  INDEX `id_contrato` (`id_contrato` ASC) VISIBLE,
  INDEX `ix_proyectos_id_proyecto` (`id_proyecto` ASC) VISIBLE,
  CONSTRAINT `proyectos_ibfk_1`
    FOREIGN KEY (`id_cliente`)
    REFERENCES `contralog`.`clientes` (`id_cliente`),
  CONSTRAINT `proyectos_ibfk_2`
    FOREIGN KEY (`id_contrato`)
    REFERENCES `contralog`.`contratos` (`id_contrato`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `contralog`.`registro_horas`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `contralog`.`registro_horas` (
  `id_registro` INT NOT NULL AUTO_INCREMENT,
  `id_usuario` INT NOT NULL,
  `id_proyecto` INT NULL DEFAULT NULL,
  `tipo_registro` ENUM('proyecto', 'novedad') NOT NULL,
  `descripcion_novedad` VARCHAR(100) NULL DEFAULT NULL,
  `fecha_trabajo` DATE NOT NULL,
  `cantidad_horas` FLOAT NOT NULL,
  `comentario` VARCHAR(255) NULL DEFAULT NULL,
  `estado_aprobacion` ENUM('pendiente', 'aprobado', 'rechazado') NULL DEFAULT NULL,
  `id_aprobador` INT NULL DEFAULT NULL,
  `fecha_aprobacion` TIMESTAMP NULL DEFAULT NULL,
  PRIMARY KEY (`id_registro`),
  INDEX `id_usuario` (`id_usuario` ASC) VISIBLE,
  INDEX `id_proyecto` (`id_proyecto` ASC) VISIBLE,
  INDEX `id_aprobador` (`id_aprobador` ASC) VISIBLE,
  INDEX `ix_registro_horas_id_registro` (`id_registro` ASC) VISIBLE,
  CONSTRAINT `registro_horas_ibfk_1`
    FOREIGN KEY (`id_usuario`)
    REFERENCES `contralog`.`usuarios` (`id_usuario`),
  CONSTRAINT `registro_horas_ibfk_2`
    FOREIGN KEY (`id_proyecto`)
    REFERENCES `contralog`.`proyectos` (`id_proyecto`),
  CONSTRAINT `registro_horas_ibfk_3`
    FOREIGN KEY (`id_aprobador`)
    REFERENCES `contralog`.`usuarios` (`id_usuario`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
